<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                 https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
             version="3.1">

    <!-- ================================================
         Unidad de persistencia del SERVIDOR (bd_servidor)
         ================================================ -->
    <persistence-unit name="servidorPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>com.example.gRPC.entity.Profesor</class>

        <properties>
            <property name="jakarta.persistence.jdbc.driver"   value="org.postgresql.Driver"/>
            <!-- En Docker usa el nombre del contenedor; fuera de Docker usa localhost -->
            <property name="jakarta.persistence.jdbc.url"      value="jdbc:postgresql://postgres-servidor:5432/Taller"/>
            <property name="jakarta.persistence.jdbc.user"     value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="password"/>

            <property name="hibernate.dialect"       value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql"      value="true"/>
            <property name="hibernate.format_sql"    value="true"/>
        </properties>
    </persistence-unit>

    <!-- ================================================
         Unidad de persistencia del CLIENTE (bd_cliente)
         ================================================ -->
    <persistence-unit name="clientePU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>com.example.gRPC.entity.ConsultaLog</class>
        <class>com.example.gRPC.entity.Profesor</class>

        <properties>
            <property name="jakarta.persistence.jdbc.driver"   value="org.postgresql.Driver"/>
            <!-- En Docker usa el nombre del contenedor; fuera de Docker usa localhost -->
            <property name="jakarta.persistence.jdbc.url"      value="jdbc:postgresql://postgres-cliente:5432/Cliente"/>
            <property name="jakarta.persistence.jdbc.user"     value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="password"/>

            <property name="hibernate.dialect"       value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql"      value="true"/>
            <property name="hibernate.format_sql"    value="true"/>
        </properties>
    </persistence-unit>

</persistence>